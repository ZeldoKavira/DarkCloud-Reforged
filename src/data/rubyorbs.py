"""Auto-generated from RubyOrbs.cs — do not edit manually."""

offset = 4



class Orb0:
    id = 0x21E97A60
    lifespan = 0x21E97A30
    damage = 0x21E97A70
    element = 0x21E97AD0


# --- Orb1 (computed from base + offset) ---
class Orb1:
    OrbMultiplier = 1
    id = Orb0.id + 2
    lifespan = Orb0.lifespan + (offset * OrbMultiplier)
    damage = Orb0.damage + (offset * OrbMultiplier)
    element = Orb0.element + (offset * OrbMultiplier)


# --- Orb2 (computed from base + offset) ---
class Orb2:
    OrbMultiplier = 2
    id = Orb0.id + 4
    lifespan = Orb0.lifespan + (offset * OrbMultiplier)
    damage = Orb0.damage + (offset * OrbMultiplier)
    element = Orb0.element + (offset * OrbMultiplier)


# --- Orb3 (computed from base + offset) ---
class Orb3:
    OrbMultiplier = 3
    id = Orb0.id + 6
    lifespan = Orb0.lifespan + (offset * OrbMultiplier)
    damage = Orb0.damage + (offset * OrbMultiplier)
    element = Orb0.element + (offset * OrbMultiplier)


# --- Orb4 (computed from base + offset) ---
class Orb4:
    OrbMultiplier = 4
    id = Orb0.id + 8
    lifespan = Orb0.lifespan + (offset * OrbMultiplier)
    damage = Orb0.damage + (offset * OrbMultiplier)
    element = Orb0.element + (offset * OrbMultiplier)


# --- Orb5 (computed from base + offset) ---
class Orb5:
    OrbMultiplier = 5
    id = Orb0.id + 10
    lifespan = Orb0.lifespan + (offset * OrbMultiplier)
    damage = Orb0.damage + (offset * OrbMultiplier)
    element = Orb0.element + (offset * OrbMultiplier)

